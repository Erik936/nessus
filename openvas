sudo -u postgres psql -d gvmd -c "
-- 1. Insert 'Full and fast' Scan Config
INSERT INTO configs (uuid, name, comment, owner) 
VALUES (
  'daba56c8-73ec-11df-a475-002264764cea', 
  'Full and fast', 
  'The recommended default configuration for most scans.', 
  (SELECT id FROM users WHERE name = 'gvm')
) ON CONFLICT (uuid) DO UPDATE SET owner = (SELECT id FROM users WHERE name = 'gvm');

-- 2. Insert 'System Discovery' Scan Config
INSERT INTO configs (uuid, name, comment, owner) 
VALUES (
  '08b55873-55c9-4503-9214-3d347b750a3e', 
  'System Discovery', 
  'Discovers target systems and open ports.', 
  (SELECT id FROM users WHERE name = 'gvm')
) ON CONFLICT (uuid) DO UPDATE SET owner = (SELECT id FROM users WHERE name = 'gvm');

-- 3. Link existing NVTs in your DB to 'Full and fast'
INSERT INTO config_preferences (config, type, name, value)
SELECT (SELECT id FROM configs WHERE uuid = 'daba56c8-73ec-11df-a475-002264764cea'), 'nvt', name, value 
FROM nvt_preferences
ON CONFLICT DO NOTHING;
"
